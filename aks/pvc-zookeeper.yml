kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: task-zookeeper-pv-claim
spec:
  storageClassName: azurefile
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
